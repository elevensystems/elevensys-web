/**
 * Shared UI constants used across multiple tool pages
 */

/** Duration in ms to show the "copied" feedback before reverting */
export const COPY_FEEDBACK_DURATION = 2000;

/** Default toast notification duration in ms */
export const TOAST_DURATION = 5000;

/** Maximum input length for text-based tools */
export const MAX_INPUT_LENGTH = 10000;
